# QuantMind RL prototype — lean core runtime (no scipy/sklearn/shap; XAI is
# implemented from scratch in NumPy to stay lightweight). Exact versions pinned
# (not >= ranges) so a fresh clone reproduces the locked results bit-for-bit;
# see requirements-optional.txt for the sentiment/news pipeline, which most
# reproduction steps (training, evaluation, tests, dashboard) do not need.
numpy==2.0.2
pandas==3.0.3
matplotlib==3.10.9
yfinance==1.4.1
gymnasium==1.2.3
stable_baselines3==2.8.0
torch==2.12.0
tqdm==4.67.3
pytest==9.1.1        # property test suite (tests/); run: python -m pytest

# ---- Serving backend (serve.py) + chat (src/rationale.py) ----
fastapi==0.139.0
uvicorn==0.51.0
pydantic==2.13.4
requests==2.34.2
